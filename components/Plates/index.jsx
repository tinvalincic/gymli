import React from 'react';
import PropTypes from 'prop-types';

const Plates = (props) => {
    return (
        <div>
            Ovdje će bit platesi
        </div>
    );
}

Plates.propTypes = {

};

export default Plates;

